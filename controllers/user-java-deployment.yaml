apiVersion: apps/v1
kind: Deployment
metadata:
  #A Deployment named user-java-deployment is created, indicated by the .metadata.name field.
  name: user-java-deployment
spec:
  replicas: 1
  #The .spec.selector field defines how the Deployment finds which Pods to manage. 
  selector:
    matchLabels:
      app: user-java
  #The template field contains the following sub-fields:
  #The Pods are labeled app: user-java using the .metadata.labels field.
  template:
    metadata:
      labels:
        app: user-java
    spec:
      containers:
      - name: user-java
        image: registryofy4959.azurecr.io/tripinsights/user-java:1.0
        #image: nginx:1.14.2
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 250m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi